# pre-commit:
#     commands:
#         lint-frontend:
#             run: cd frontend && pnpm run lint $(echo "{staged_files}" | sed 's/ /\n/g' | grep "^frontend/" | sed 's/^frontend\///')
#         prettier-frontend:
#             run: cd frontend && pnpm run prettier --check $(echo "{staged_files}" | sed 's/ /\n/g' | grep "^frontend/" | sed 's/^frontend\///')

# commit-msg:
#     commands:
#         lint:
#             run: npx commitlint --edit
# pre-push:
#     commands:
#         test:
#             glob: '*.{js,ts,jsx,tsx}'
#             run: npm run test related -- --run {push_files}
